#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>
int main()
{
	char arr1[] = "hiahiahia";
	memset(arr1, '#', 5);
	//memory--内存   set--设置
	//memset-更改存储的地址，存进去的为ascll值
	printf("%s\n", arr1);	
	return 0;
}
