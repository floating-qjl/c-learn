#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <string.h>

//#include 指令可以使另一个文件被编译，就像它实际出现于 #include 指令的地方一样
//这种替换方式很简单：预处理器先删除这条指令，并用包含文件的内容替换，这样一个源文件被包含十次，那就实际被编译10此

//头文件包含的方式：1.本地文件包含 - 用 ""
//         查找策略： 现在源文件所在目录下查找，如果未找到，编译器就像查找库函数头文件一样在标准位置（安装目录）
//                    查找头文件，如果找不到就会提示错误
//                  2.库文件包含 - 用 <>
//         查找策略：查找头文件直接去标准路径下去查找，如果找不到就提示编译错误
//                   很明显，库文件也可以使用""的形式查找，但这样做查找的效率就会变低，也不宜区分库文件和本地文件



//#ifndef __TEST_H__ 如果未定义(此处引用的头文件为 "test.h" )
//#define __TEST_H__ 进行定义
//函数声明
//#endif
//这段代码可以有效 防止多次引用头文件 而造成时间浪费


//#pragma once
// 函数声明
//这段代码同样可以 解决多次引用头文件 的问题
int main()
{

	return 0;
}
