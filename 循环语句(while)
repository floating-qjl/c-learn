#include<stdio.h>
#include<string.h>
int main()
{
	int a = 0;
	while (a < 10)
	{
		a++;
		if(a==5)
		continue;
		printf("%d\n", a);
	}
	while (a >=10)
	{
		a++;
		printf("%d\n", a);
		if (a ==20)
			break;
	}	
	return 0;
}
