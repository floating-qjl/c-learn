#include<stdio.h>
#include<string.h>
int main()
{
	int a = 0;
	while (a < 10)
	{
		a++;
		if(a==5)
		continue;
		//continue--结束当前循环，并从判断开始下一循环
		printf("%d\n", a);
	}
	while (a >=10)
	{
		a++;
		printf("%d\n", a);
		if (a ==20)
			break;
			//braek--结束循环
	}	
	return 0;
}
