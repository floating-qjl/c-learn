#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <math.h>
#include <string.h>
int main()
{
	float f = 5.5;
	//101.1 - 二进制
	//（-1）^0 * 1.011 * 2^2
	//S - 0
	//M - 1.011 
	//E - 2
	//E的实际储存值为 2 + 127 = 129
	//0  10000001  01100000000000000000000 -- 二进制存储
	//0x40b00000 - 十六进制  地址

	//如果E为全0，即 E 为 -127
	//换算为  （+-）1.xxxxxx * 2^(-127) 数字过小接近于 0
	//此时小数点前面不补1，而是补0，后面指数为 1-中间值（127或1023）
	//如 (-1)^0 * 0.xxxxx * 2 ^(-126)
	//表示 0

	//如果E全为1，即 E 为 128
	//换算为    (+-) 1.xxxxx * 2^(128)  数字过大
	//表示的是 正负无穷大的数字
	return 0;
}
